# E-Tool 开发日志

## [2026-02-06]

### 已完成修改
1. **i18n 翻译工作**:
   - 完成了 `js/translations_data.js` 的初步翻译，涵盖 800+ 词条。
   - 修复了语言切换后部分文本仍显示中文的问题。
   - 调整了 `index.html` 中脚本加载顺序，确保 i18n 资源优先加载。

2. **识别功能初步优化 (recognition.js)**:
   - 扩展了噪声词库 (`noiseWords`)，减少靶基因误报。
   - 增加了载体标准化映射（如 `pLKO.1` -> `pLKO`, `pcDNA3.1` -> `pcDNA3`）。
   - 引入了特殊载体推理逻辑（如识别 `psPAX2` 自动关联 `gag`, `pol` 等基因）。
   - 优化了物种推断规则，解决了 `psPAX2` 被误判为“人”的问题。
   - 建立了基于网络数据的测试集 `web_plasmids.json`，当前平均识别准确率为 72.22%。

### 待做任务 (TODO)
- [ ] **英文显示优化 (高优先级)**:
  - [ ] 翻译 `index.html` 中的残留中文注释及 CSS 提示。
  - [ ] 优化 `locales/en.json` 中的占位符翻译（如 `[EN]` 前缀）。
  - [ ] 确保所有界面提示（Prompt/Alert）在英文模式下语义准确。
- [ ] **识别功能深度优化 (待做 - 待准备完整质粒库)**:
  - [ ] 解决载体误判为 `pAc5.1` 的问题（目前 `pSpCas9` 仍有此类误判）。
  - [ ] 进一步优化 `vectorPatterns` 优先级。
  - [ ] 完善 `pTeton`, `pCo`, `pUAST` 等载体的识别规则。
  - [ ] 改进物种推断逻辑，减少人/哺乳动物误判。
